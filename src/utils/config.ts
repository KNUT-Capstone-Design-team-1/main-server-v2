export const CONFIG = {
  DB_UPDATE_DATE: 'db.update-date',
} as const;

export const CONFIG_TYPE = {
  STRING: 'string',
  STRING_ARRAY: 'string-array',
  NUMBER: 'number',
  NUMBER_ARRAY: 'number-array',
} as const;
